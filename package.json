{
  "scripts": {
    "dev": "next",
    "start": "next start",
    "build": "next build",
    "export": "yarn run build && next export", 
    "format": "npx prettier --write \"**/*.js\" && (npx eslint ./ --fix || exit 0)"
  },
  "dependencies": {
    "@zeit/next-sass": "^1.0.1",
    "next": "^8.1.0",
    "next-offline": "^4.0.1",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-react": "^7.13.0",
    "prettier": "^1.17.1"
  }
}
